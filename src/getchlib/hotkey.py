class HotKeyListener:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "Hotkeys have been removed in getchlib>=2.0.0. Please revert to an earlier version."
        )
